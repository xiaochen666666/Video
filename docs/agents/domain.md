# Domain docs

Read root `CONTEXT.md` and relevant `docs/adr/` decisions before domain changes. If absent, continue silently; create these documents only when terms or decisions are resolved. Use glossary terminology and explicitly flag conflicts with existing ADRs. This is a single-context project.
