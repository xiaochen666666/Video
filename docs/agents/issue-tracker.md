# Issue tracker: GitHub

Repository: `xiaochen666666/Video`. Use `gh issue` to read, list, create and update issues. Read comments when fetching a ticket. For multiline bodies use `--body-file` with a temporary UTF-8 file. Creating or commenting on remote issues requires user authorization.

PRs as a request surface: no.
