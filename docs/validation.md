# 验收记录

日期：2026-09-12。环境：Windows、Ryzen 7 5800H、约 16 GB RAM。所有付费模型调用均未执行，因为尚未配置用户的百炼 API Key。

## 已完成

- TypeScript 检查和 Vite 生产构建通过。
- 20 项后端自动化测试通过，覆盖：密钥脱敏、跨站请求拒绝、链接校验、任务复用、Markdown/ZIP 图片导出、历史删除、401/403/402、限流、服务错误、超时、连接错误的有界重试，以及请求中的图片/音频格式。
- 工作队列测试验证：速览在章节前可读、失败后检查点恢复且不重复成功请求、取消任务、启动时恢复运行任务。
- 无音轨视频跳过语音识别并按画面总结；损坏检查点不会阻塞后续任务。
- 真实 FFmpeg 测试验证：视频时长、音频片段不超过 30 秒、音频大小限制、实际抽帧和图片文件生成。
- 真实浏览器检查：首页、设置页、未配置 Key 的暂停提示及历史删除；桌面和 390px 窄屏结果页正常显示，控制台无错误。
- 公开平台元数据探测成功：YouTube `jNQXAC9IVRw`（Me at the zoo，19 秒，英文人工字幕）；B站 `BV1xx411c7mD`（字幕君交流场所，2055.637 秒，无可用字幕）。元数据可读取不等同于全部视频下载或真实模型分析通过。
- YouTube 短视频下载实测通过：下载英文字幕并解析 6 段，实际视频时长 19.02 秒，抽帧成功。结果保存在 `data/download-smoke.json`，没有上传至云端模型。

## 合成视频端到端基准

使用包含 PPT、柱状图和操作界面的自制测试视频。FFmpeg 预处理真实执行，模型响应为固定模拟数据；数值不代表云端模型性能。

| 样例 | 字幕 | 首次速览 | 总处理时间 | 章节 / 关键帧 | 模拟调用数 | 真实 API 费用 |
|---|---|---:|---:|---:|---:|---:|
| 10 分钟 | 有 | 0.02 秒 | 3.62 秒 | 2 / 30 | 11 | 0 |
| 60 分钟 | 无，120 个音频片段 | 14.51 秒 | 37.34 秒 | 12 / 179 | 171 | 0 |

时间从处理任务开始计算，不含生成合成视频。结果位于 `data/benchmark/results.json`。合成视频帧率低、画面简单，因此真实视频预处理可能更慢。

## 需要有效 Key 后继续验证

- 北京账户对 `qwen3.5-flash`、`qwen3-asr-flash` 的真实调用权限及用量返回。
- 真实 B站和 YouTube 长视频完整下载、字幕获取、画面理解和语音识别质量。
- 对照实际 PPT、图表数值和操作步骤人工检查笔记准确性，确认时间戳回看位置。
- 10 / 60 分钟真实视频的首次速览、总耗时及实际费用。不能把上面的模拟结果当作真实 API 验收。

当前实现不会替用户开通账户、充值或提交远程 GitHub Issues。站点登录限制、区域限制或反爬变化可能需要用户主动导入 cookies，或使用本地视频入口。
